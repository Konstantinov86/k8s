---
    apiVersion: v1
    kind: Service
    metadata:
      name: my-service-secret
    spec:
      ports:
      - port: 80
        targetPort: 80
      selector:
        app: my-app-secret
      type: ClusterIP