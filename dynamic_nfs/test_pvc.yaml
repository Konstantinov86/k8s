apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-pvc-2
  namespace: test
spec:
  storageClassName: nfs-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Mi