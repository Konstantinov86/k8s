---
    apiVersion: v1
    kind: Service
    metadata:
      name: my-service2nodeport
    spec:
      type: NodePort
      ports:
      - port: 80
        targetPort: 80
      selector:
        app: my-app
 